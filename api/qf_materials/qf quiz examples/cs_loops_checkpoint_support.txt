<QUIZFORGE_JSON>
{
  "version": "3.0-json",
  "title": "Loops & Variables Checkpoint",
  "instructions": "<p>Read the short program line by line before you answer. Remember: Python counting starts at <strong>0</strong>. Use the word banks and choices provided.</p>",
  "metadata": {
    "subject": "Intro to Computer Science",
    "grade": 8,
    "standard": "Trace and predict output of simple Python for loops; identify valid variable names.",
    "tier": 1,
    "variant_group": "cs_loops_checkpoint",
    "variant_label": "Support"
  },
  "items": [
    {
      "id": "stim_for",
      "type": "STIMULUS",
      "format": "code",
      "prompt": "<pre style='background-color:#272822;color:#F8F8F2;padding:10px;border-radius:4px;font-family:Courier New, Consolas, monospace;white-space:pre;display:block;'><code>for i in range(3):\n    print(i)</code></pre>"
    },
    {
      "id": "mc_for_output",
      "type": "MC",
      "stimulus_id": "stim_for",
      "prompt": "<p>What does the program above print? (Hint: counting starts at 0.)</p>",
      "choices": [
        { "id": "A", "text": "0, then 1, then 2 (each on its own line)", "correct": true },
        { "id": "B", "text": "1, then 2, then 3 (each on its own line)", "correct": false },
        { "id": "C", "text": "0 1 2 all on a single line", "correct": false }
      ]
    },
    {
      "id": "tf_range3",
      "type": "TF",
      "stimulus_id": "stim_for",
      "prompt": "<p><code>range(3)</code> includes the value 3.</p>",
      "answer": false
    },
    {
      "id": "num_runs",
      "type": "NUMERICAL",
      "prompt": "<p>The values of <code>range(5)</code> are 0, 1, 2, 3, 4. How many times does the loop body run in <code>for i in range(5):</code>?</p>",
      "answer": 5,
      "evaluation": { "mode": "exact", "value": 5 }
    },
    {
      "id": "ma_varnames",
      "type": "MA",
      "prompt": "<p>A valid Python variable name may use letters, digits, and underscores, but cannot start with a digit or contain a space. Which of these are valid? Select all that apply.</p>",
      "choices": [
        { "id": "A", "text": "_count", "correct": true },
        { "id": "B", "text": "total2", "correct": true },
        { "id": "C", "text": "2cool", "correct": false },
        { "id": "D", "text": "my var", "correct": false }
      ]
    },
    {
      "id": "fitb_keyword",
      "type": "FITB",
      "prompt": "<p>Python uses the [blank] keyword to begin a counting loop.</p>",
      "answer_mode": "wordbank",
      "accept": ["for"],
      "options": ["for", "print", "input"],
      "case_sensitive": false
    },
    { "id": "stim_for_end", "type": "STIMULUS_END", "prompt": "" }
  ],
  "rationales": [
    {
      "item_id": "mc_for_output",
      "choices": [
        { "id": "A", "correct": true, "rationale": "In Python, range(n) starts counting at 0 and stops right before n. Here range(3) gives 0, 1, 2, and print() puts each one on its own line." },
        { "id": "B", "correct": false, "rationale": "In Python, range(n) starts counting at 0 and stops right before n. This choice wrongly starts at 1, but range(3) actually begins at 0 and gives 0, 1, 2." },
        { "id": "C", "correct": false, "rationale": "Each call to print() adds a newline, so its output always starts a fresh line. That means the three values print on separate lines, never squeezed onto a single line." }
      ]
    },
    { "item_id": "tf_range3", "rationale": "In Python, range(n) stops right before n, so the value n itself is never produced. This statement is false: range(3) gives 0, 1, 2, and 3 is never included." },
    { "item_id": "num_runs", "rationale": "In Python, range(n) produces exactly n values, so the loop body runs n times total. Here range(5) gives five values, 0 through 4, so the body runs five times." },
    {
      "item_id": "ma_varnames",
      "choices": [
        { "id": "A", "correct": true, "rationale": "A valid Python name may use letters, digits, and underscores, as long as it does not start with a digit. _count starts with an underscore, not a digit, so it is a valid name." },
        { "id": "B", "correct": true, "rationale": "A valid Python name may use letters, digits, and underscores, as long as it does not start with a digit. total2 starts with a letter and only adds digits after, so it is valid." },
        { "id": "C", "correct": false, "rationale": "A valid Python name can never begin with a digit, since Python would read that as a number. 2cool starts with the digit 2, so it is not a valid variable name." },
        { "id": "D", "correct": false, "rationale": "A valid Python name can never contain a space, since Python would read it as two separate words. my var has a space in the middle, so it is not a valid variable name." }
      ]
    },
    { "item_id": "fitb_keyword", "rationale": "In Python, a for loop repeats its body once for each value in a sequence. The keyword that starts this kind of loop, and belongs in the blank, is for." }
  ]
}
</QUIZFORGE_JSON>
