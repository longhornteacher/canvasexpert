<QUIZFORGE_JSON>
{
  "version": "3.0-json",
  "title": "Loops & Variables Checkpoint",
  "instructions": "<p>Trace the program carefully and be ready to justify your reasoning with evidence from the code.</p>",
  "metadata": {
    "subject": "Intro to Computer Science",
    "grade": 8,
    "standard": "Trace and predict output of simple Python for loops; identify valid variable names.",
    "tier": 4,
    "variant_group": "cs_loops_checkpoint",
    "variant_label": "Extend"
  },
  "items": [
    {
      "id": "stim_accum",
      "type": "STIMULUS",
      "format": "code",
      "prompt": "<pre style='background-color:#272822;color:#F8F8F2;padding:10px;border-radius:4px;font-family:Courier New, Consolas, monospace;white-space:pre;display:block;'><code>total = 0\nfor n in range(1, 4):\n    total = total + n\nprint(total)</code></pre>"
    },
    {
      "id": "mc_accum_output",
      "type": "MC",
      "stimulus_id": "stim_accum",
      "prompt": "<p>What does this program print?</p>",
      "choices": [
        { "id": "A", "text": "6", "correct": true },
        { "id": "B", "text": "3", "correct": false },
        { "id": "C", "text": "10", "correct": false },
        { "id": "D", "text": "0", "correct": false }
      ]
    },
    {
      "id": "ma_accum_true",
      "type": "MA",
      "stimulus_id": "stim_accum",
      "prompt": "<p>Which statements about this program are true? Select all that apply.</p>",
      "choices": [
        { "id": "A", "text": "n takes the values 1, 2, and 3", "correct": true },
        { "id": "B", "text": "The loop body runs three times", "correct": true },
        { "id": "C", "text": "range(1, 4) includes the value 4", "correct": false },
        { "id": "D", "text": "total is reset to 0 on every pass of the loop", "correct": false }
      ]
    },
    {
      "id": "num_accum_total",
      "type": "NUMERICAL",
      "stimulus_id": "stim_accum",
      "prompt": "<p>What is the value of <code>total</code> after the loop finishes?</p>",
      "answer": 6,
      "evaluation": { "mode": "exact", "value": 6 }
    },
    {
      "id": "fitb_accum_var",
      "type": "FITB",
      "stimulus_id": "stim_accum",
      "prompt": "<p>The variable that keeps a running sum across the loop is named [blank].</p>",
      "accept": ["total"],
      "answer_mode": "open_entry",
      "case_sensitive": true
    },
    {
      "id": "cat_keywords",
      "type": "CATEGORIZATION",
      "prompt": "<p>Sort each Python keyword into the correct category.</p>",
      "categories": ["Loops", "Conditionals"],
      "items": [
        { "label": "for", "category": "Loops" },
        { "label": "while", "category": "Loops" },
        { "label": "if", "category": "Conditionals" },
        { "label": "elif", "category": "Conditionals" }
      ]
    },
    { "id": "stim_accum_end", "type": "STIMULUS_END", "prompt": "" }
  ],
  "rationales": [
    {
      "item_id": "mc_accum_output",
      "choices": [
        { "id": "A", "correct": true, "rationale": "An accumulator variable keeps a running sum by adding each new value to what it already holds. Here total adds 1, then 2, then 3, ending at 6, which is what prints." },
        { "id": "B", "correct": false, "rationale": "An accumulator variable keeps a running sum by adding each new value to what it already holds. This choice shows only the last value added, but total keeps the running sum of all of them." },
        { "id": "C", "correct": false, "rationale": "An accumulator variable keeps a running sum by adding each new value to what it already holds. This choice adds one value too many, but range(1, 4) stops before reaching 4." },
        { "id": "D", "correct": false, "rationale": "An accumulator variable keeps a running sum by adding each new value to what it already holds. This choice shows the starting value before the loop runs, not the value after it finishes." }
      ]
    },
    {
      "item_id": "ma_accum_true",
      "choices": [
        { "id": "A", "correct": true, "rationale": "In Python, range(start, stop) counts up from start and stops right before stop. Here range(1, 4) yields 1, 2, 3, and n takes each of those values in turn." },
        { "id": "B", "correct": true, "rationale": "A loop's body runs exactly once for every value the range actually produces. Here range(1, 4) produces three values, so the loop body runs three times." },
        { "id": "C", "correct": false, "rationale": "In Python, range(start, stop) counts up from start and stops right before stop. This choice wrongly claims 4 is included, but range always stops before its stop argument." },
        { "id": "D", "correct": false, "rationale": "A line placed before a loop begins runs only once, not again on every pass. Here total = 0 runs once before the loop starts, not on each pass through it." }
      ]
    },
    { "item_id": "num_accum_total", "rationale": "An accumulator variable keeps a running sum by adding each loop value to what it already holds. Here total adds 1, 2, and 3 to its starting 0, ending at 6." },
    { "item_id": "fitb_accum_var", "rationale": "An accumulator variable stores a running total that gets updated on every pass of a loop. Here that variable is named total, updated each pass with total = total + n." },
    { "item_id": "cat_keywords", "rationale": "Python keywords fall into groups: some repeat a block of code, while others choose between paths. Here for and while are loops, while if and elif are conditionals that pick a branch." }
  ]
}
</QUIZFORGE_JSON>
