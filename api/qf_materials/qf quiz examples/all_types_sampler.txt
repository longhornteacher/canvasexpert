<QUIZFORGE_JSON>
{
  "version": "3.0-json",
  "title": "All Question Types Sampler (CS)",
  "instructions": "<p>This quiz includes one of every QuizForge question type. It is a fixture for testing the QuizForge-to-Canvas pipeline.</p>",
  "metadata": {
    "subject": "Intro to Computer Science",
    "grade": 8,
    "standard": "Mixed - fixture exercising every QuizForge item type.",
    "variant_group": "all_types_sampler",
    "variant_label": "Coverage fixture"
  },
  "items": [
    {
      "id": "stim_snippet",
      "type": "STIMULUS",
      "format": "code",
      "prompt": "<pre style='background-color:#272822;color:#F8F8F2;padding:10px;border-radius:4px;font-family:Courier New, Consolas, monospace;white-space:pre;display:block;'><code>x = 5\nprint(x * 2)</code></pre>"
    },
    {
      "id": "mc_snippet_out",
      "metadata": { "teks": "126.34(c)(4)(A)" },
      "type": "MC",
      "stimulus_id": "stim_snippet",
      "prompt": "<p>What does the program above print?</p>",
      "choices": [
        { "id": "A", "text": "10", "correct": true },
        { "id": "B", "text": "52", "correct": false },
        { "id": "C", "text": "5 * 2", "correct": false }
      ]
    },
    { "id": "stim_snippet_end", "type": "STIMULUS_END", "prompt": "" },
    {
      "id": "tf_indent",
      "type": "TF",
      "prompt": "<p>In Python, indentation is part of the syntax and changes how code runs.</p>",
      "answer": true
    },
    {
      "id": "ma_outputs",
      "metadata": { "teks": "126.34(c)(5)(A)" },
      "type": "MA",
      "prompt": "<p>Which of the following are ways a program can show information to a user? Select all that apply.</p>",
      "choices": [
        { "id": "A", "text": "print() to the screen", "correct": true },
        { "id": "B", "text": "Writing text to a file", "correct": true },
        { "id": "C", "text": "Declaring a variable", "correct": false },
        { "id": "D", "text": "Adding a comment with #", "correct": false }
      ]
    },
    {
      "id": "match_symbols",
      "type": "MATCHING",
      "prompt": "<p>Match each Python symbol to its meaning.</p>",
      "pairs": [
        { "left": "=", "right": "Assigns a value to a variable" },
        { "left": "==", "right": "Checks whether two values are equal" },
        { "left": "+", "right": "Adds two numbers" }
      ],
      "distractors": ["Checks whether two values are NOT equal"]
    },
    {
      "id": "fitb_variable",
      "metadata": { "teks": "126.34(c)(4)(A)" },
      "type": "FITB",
      "prompt": "<p>A [blank] is a named container that stores a value you can use later.</p>",
      "accept": ["variable"],
      "answer_mode": "open_entry",
      "case_sensitive": false
    },
    {
      "id": "num_modulo",
      "metadata": { "teks": "126.34(c)(4)(C)" },
      "type": "NUMERICAL",
      "prompt": "<p>In Python, what is the result of <code>7 % 3</code>?</p>",
      "answer": 1,
      "evaluation": { "mode": "exact", "value": 1 }
    },
    {
      "id": "order_steps",
      "type": "ORDERING",
      "header": "First step at top, last step at bottom",
      "prompt": "<p>Put the steps of writing and running a Python program in the correct order.</p>",
      "items": [
        "Write the code in the editor",
        "Save the file",
        "Run the program",
        "Read the output"
      ]
    },
    {
      "id": "cat_tokens",
      "metadata": { "teks": "126.34(c)(4)(A)" },
      "type": "CATEGORIZATION",
      "prompt": "<p>Sort each Python piece into the correct group.</p>",
      "categories": ["Keyword", "Operator"],
      "items": [
        { "label": "for", "category": "Keyword" },
        { "label": "if", "category": "Keyword" },
        { "label": "+", "category": "Operator" },
        { "label": "==", "category": "Operator" }
      ]
    }
  ],
  "rationales": [
    {
      "item_id": "mc_snippet_out",
      "choices": [
        { "id": "A", "correct": true, "rationale": "Python evaluates an expression's variables and operators to produce a single result before printing it. x holds 5, so 5 * 2 evaluates to 10, which the program prints." },
        { "id": "B", "correct": false, "rationale": "Python evaluates an expression's variables and operators to produce a single result before printing it. 52 looks like joining the digits 5 and 2 as text, but x is a number, so Python multiplies them." },
        { "id": "C", "correct": false, "rationale": "Python evaluates an expression's variables and operators to produce a single result before printing it. 5 * 2 is the code's own text, but Python evaluates it and prints the result, 10." }
      ]
    },
    { "item_id": "tf_indent", "rationale": "In Python, indentation is not just style; it defines which lines belong to a code block. This statement is true: changing indentation changes which lines run together and how the program behaves." },
    {
      "item_id": "ma_outputs",
      "choices": [
        { "id": "A", "correct": true, "rationale": "A program's output is any information it sends where a user can see or access it. print() sends text straight to the screen, so it counts as output the user sees." },
        { "id": "B", "correct": true, "rationale": "A program's output is any information it sends where a user can see or access it. Writing to a file also sends information somewhere the user can later open and read." },
        { "id": "C", "correct": false, "rationale": "A program's output is any information it sends where a user can see or access it. Declaring a variable only stores a value in memory; it shows nothing to the user." },
        { "id": "D", "correct": false, "rationale": "A program's output is any information it sends where a user can see or access it. Comments are ignored when the program runs, so they never appear anywhere the user looks." }
      ]
    },
    { "item_id": "match_symbols", "rationale": "Python uses different symbols for assignment, comparison, and arithmetic, and each one has one specific job. Here = assigns, == checks equality, + adds, and the unlisted != checks for inequality instead." },
    { "item_id": "fitb_variable", "rationale": "A variable is a named container that stores a value so a program can use it later. The blank names exactly that idea, so the correct word to fill in is variable." },
    { "item_id": "num_modulo", "rationale": "The % operator, called modulo, returns the remainder left over after one number divides another. Dividing 7 by 3 goes in twice with 1 left over, so the answer is 1." },
    { "item_id": "order_steps", "rationale": "Running a program follows a fixed order: you cannot see output before the code has actually run. Here that order is write the code, save the file, run the program, then read the output." },
    { "item_id": "cat_tokens", "rationale": "Python code is built from keywords that control structure and operators that act on values. Here for and if are keywords, while + and == are operators, matching each group correctly." }
  ]
}
</QUIZFORGE_JSON>
